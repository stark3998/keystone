export class UserSimulator {
        // Dictionary to store user mac addresses and their corresponding metadata
        private static userLocations: { [macAddress: string]: { x: number, y: number, nap: number } } = {};

        public static initializeUsers(){

        }

        public static updateUserlocation(){

        }

        private findNearestAP(floorPlan: any){

        }

        private calculateEuclideanDistance(p1: { x: number, y: number, nap: number }, p2: { x: number, y: number, nap: number }){

        }

}